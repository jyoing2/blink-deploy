import React, { useRef, useEffect } from "react";
import styled from "styled-components";

const HomeContainer = styled.div`
  width: 100%;
  height: 100%;
`;

export default function Home() {
  return <HomeContainer>안녕</HomeContainer>;
}
